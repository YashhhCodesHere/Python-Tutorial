tup = (197, 12, 85, 99, "Shivam", "Rohan") 
empty = () # Creates an empty tuple

print(empty)
print(type(tup))
print(tup)

a,b,c,d,e,f = tup # Tuples unpacking - Values of tuples are stored into a,b,c,d,e,f (Variables must be as much as the number of indexes in the tuples.)

print(a,c,d)